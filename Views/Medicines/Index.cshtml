@model IEnumerable<PharmacyManagementSystem.Models.Medicine>
<section class="page-head"><div><span class="eyebrow">Operations</span><h1>Medicine records</h1></div><a class="button" href="/Medicines/Create">Add record</a></section>@if (TempData["Notice"] is not null) { <div class="notice">@TempData["Notice"]</div> }<form class="filters" method="get"><input name="search" value="@ViewBag.Search" placeholder="Search by name"><select name="status"><option value="">All statuses</option><option>Active</option><option>Pending</option><option>Archived</option></select><button class="button" type="submit">Filter</button></form><div class="table-wrap"><table><thead><tr><th>Name</th><th>Status</th><th>Created</th><th></th></tr></thead><tbody>@foreach (var item in Model) { <tr><td><strong>@item.Name</strong><br><small>@item.Description</small></td><td><span class="status">@item.Status</span></td><td>@item.CreatedAt.ToLocalTime().ToString("dd MMM yyyy")</td><td><a href="/Medicines/Edit/@item.Id">Edit</a> · <a href="/Medicines/Delete/@item.Id">Delete</a></td></tr> }</tbody></table></div>